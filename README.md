# Finansia AI Assistant: RAG Chatbot

โปรเจกต์นี้เป็น Chatbot ที่ปรึกษาการเงินที่พัฒนาขึ้นสำหรับโจทย์ AI/ML Intern Take-Home Assignment โดยใช้เทคนิค RAG (Retrieval-Augmented Generation) เพื่อให้คำตอบที่อ้างอิงจากเอกสารข้อมูลจริง ลดปัญหาการมั่วข้อมูล (Hallucination)

## 🛠️ Tech Stack
* **Framework:** Streamlit (Web UI)
* **LLM:** Google Gemini (model: `gemini-flash-latest`)
* **RAG Engine:** LangChain
* **Vector Store:** FAISS
* **Embeddings:** HuggingFace (`all-MiniLM-L6-v2`)

## ⚙️ วิธีการติดตั้งและรันโปรแกรม (Setup Instructions)

1.  **Clone หรือดาวน์โหลดโปรเจกต์**
2.  **ติดตั้ง Libraries ที่จำเป็น:**
    ```bash
    pip install streamlit langchain-google-genai langchain-community faiss-cpu sentence-transformers chromadb
    ```
3.  **เตรียมไฟล์ข้อมูล:**
    * นำไฟล์ `.txt` หรือ `.pdf` ที่ต้องการเป็นฐานข้อมูล ไปใส่ในโฟลเดอร์ `data/`
4.  **รันโปรแกรม:**
    ```bash
    python -m streamlit run app.py
    ```

## 🧠 Model Choice & Reasoning
ผมเลือกใช้ **Google Gemini (Flash Model)** ผ่าน `LangChain` โดยมีเหตุผลดังนี้:
1.  **Cost-Effective:** เป็นโมเดลที่มี Free Tier ให้ใช้งาน (1,500 requests/day) เหมาะสำหรับการพัฒนาและทดสอบโดยไม่มีค่าใช้จ่ายแฝง
2.  **Performance:** มีความเร็วในการตอบสนองสูง (Low Latency) เหมาะสำหรับงาน Chatbot
3.  **Context Window:** รองรับ Context ขนาดใหญ่ ทำให้สามารถใส่ข้อมูลอ้างอิง (Context) ลงไปใน Prompt ได้จำนวนมาก

## 📚 How Knowledge Grounding Works (RAG Pipeline)
การทำงานของระบบแบ่งเป็น 2 ส่วนหลัก:
1.  **Indexing Phase (เตรียมข้อมูล):**
    * โหลดเอกสารจากโฟลเดอร์ `data/`
    * แบ่งข้อความ (Chunking) เป็นชิ้นย่อยขนาด 1000 ตัวอักษร
    * แปลงข้อความเป็นเวกเตอร์ (Embeddings) ด้วย `all-MiniLM-L6-v2` และเก็บลงใน FAISS Vector Store
2.  **Retrieval & Generation Phase (ตอบคำถาม):**
    * เมื่อผู้ใช้ถามคำถาม ระบบจะค้นหา Chunk ที่เกี่ยวข้องที่สุดจาก FAISS
    * นำข้อมูลเหล่านั้นมาใส่ใน Prompt (Context)
    * ส่งให้ Gemini สรุปคำตอบ โดยมีคำสั่งกำกับว่า "ห้ามตอบถ้าไม่มีข้อมูลใน Context"